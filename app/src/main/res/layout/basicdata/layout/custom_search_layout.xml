<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/search_main">

    <android.support.v7.widget.Toolbar
        android:id="@+id/custom_search_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:layout_scrollFlags="scroll|enterAlways|snap"
        >
        <com.example.androiderp.ui.CHomeSearch
            android:id="@+id/home_custom_search"
            android:drawableLeft="@drawable/customlist_search"
            style="@style/home_search_Style"
            />
    </android.support.v7.widget.Toolbar>
    <ListView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="#c9cabb"
        android:dividerHeight="1px" >
    </ListView>

</LinearLayout>

